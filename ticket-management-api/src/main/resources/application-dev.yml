spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Telecom
    username: bannournefzi
    password: bannour123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: nefzi.bannour19@gmail.com
    password: ezjw lqut iery phdr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8088
          jwk-set-uri: http://localhost:8088/.well-known/jwks.json

server:
  port: 8088

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha

# ─────────────────────────────────────────────
# Custom application properties
# ─────────────────────────────────────────────
application:
  security:
    jwt:
      secret-key: WU9VUlNFQ1JFVEtFWU1VU1RCRUJBU0U2NEJJVFNFQ1JFVA==
      expiration: 86400000

  file:
    uploads:
      media-output-path: uploads/media
      max-file-size: 5242880
      allowed-extensions: jpg,jpeg,png,gif,mp4,avi,mkv,wav,mp3

  mailing:
    frontend:
      activation-url: http://localhost:4200/activate-account

  websocket:
    allowed-origins: http://localhost:4200
    endpoint: /ws
    message-broker:
      enabled: true
      simple-broker: /user
      application-destination-prefix: /app
      user-destination-prefix: /user

  mantis:
    base-url: http://localhost:8989
    api-token: Za83rlw9IhObreJA5CcGYjXfE1zfjDtA
    project-id: 1
    sync:
      enabled: true
      interval-minutes: 5
      batch-size: 50


logging:
  level:
    tn.esprit.ticketmanagement.mantis: WARN