version: "3.8"

services:
  backend:
    container_name: ticket-backend
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
    ports:
      - "8088:8088"
    environment:
      SPRING_PROFILES_ACTIVE: dev

      DB_URL: jdbc:postgresql://host.docker.internal:5432/Telecom
      DB_USERNAME: bannournefzi
      DB_PASSWORD: bannour123

    restart: unless-stopped

networks:
  spring-demo:
    driver: bridge